name: Deploy MovieNight to Staging (Helm)

on:
  push:
    branches:
      - staging
    paths:
      - 'MovieNight/**'
      - '.github/workflows/movienight-staging.yml'
  workflow_dispatch:
    inputs:
      deployment_action:
        description: 'Deployment action'
        required: true
        type: choice
        options:
          - deploy
          - build-only
        default: 'deploy'

env:
  AWS_REGION: us-east-1
  ECR_REPOSITORY: int-production-use1-ecs
  EKS_CLUSTER_NAME: int-production-use1-prod-novutech-eks-cluster
  K8S_NAMESPACE: movienight-staging
  IAM_ROLE: arn:aws:iam::271457809232:role/int-prod-OIDCGitHubRole-role
  ENVIRONMENT: staging

permissions:
  id-token: write
  contents: read

jobs:
  build-and-deploy:
    name: Build and Deploy to Staging
    runs-on: ubuntu-latest
    environment: staging
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Configure AWS credentials
      uses: aws-actions/configure-aws-credentials@v4
      with:
        role-to-assume: ${{ env.IAM_ROLE }}
        role-session-name: GitHubActions-MovieNight-Staging-${{ github.run_id }}
        aws-region: ${{ env.AWS_REGION }}

    - name: Login to Amazon ECR
      id: login-ecr
      uses: aws-actions/amazon-ecr-login@v2

    - name: Get AWS Account ID
      id: account-id
      run: |
        echo "account_id=$(aws sts get-caller-identity --query Account --output text)" >> $GITHUB_OUTPUT

    - name: Get latest staging version and auto-increment
      id: version
      run: |
        echo "🔍 Checking latest staging version in ECR..."
        
        # Get all staging version tags
        VERSIONS=$(aws ecr describe-images \
          --repository-name ${{ env.ECR_REPOSITORY }} \
          --region ${{ env.AWS_REGION }} \
          --query 'imageDetails[*].imageTags[]' \
          --output text | tr '\t' '\n' | grep -E '^movienight-backend-staging-v[0-9]+\.[0-9]+\.[0-9]+$' | sed 's/movienight-backend-staging-v//' || echo "")
        
        if [ -z "$VERSIONS" ]; then
          NEW_VERSION="v1.0.0"
          echo "📌 No previous staging versions. Starting with ${NEW_VERSION}"
        else
          LATEST_VERSION=$(echo "$VERSIONS" | sort -V | tail -n 1)
          echo "📌 Latest staging version: v${LATEST_VERSION}"
          
          MAJOR=$(echo $LATEST_VERSION | cut -d. -f1)
          MINOR=$(echo $LATEST_VERSION | cut -d. -f2)
          PATCH=$(echo $LATEST_VERSION | cut -d. -f3)
          NEW_PATCH=$((PATCH + 1))
          NEW_VERSION="v${MAJOR}.${MINOR}.${NEW_PATCH}"
          echo "🚀 Auto-incrementing to: ${NEW_VERSION}"
        fi
        
        echo "version=${NEW_VERSION}" >> $GITHUB_OUTPUT

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3

    - name: Build and push backend image
      uses: docker/build-push-action@v5
      with:
        context: ./MovieNight/backend
        push: true
        tags: |
          ${{ steps.account-id.outputs.account_id }}.dkr.ecr.${{ env.AWS_REGION }}.amazonaws.com/${{ env.ECR_REPOSITORY }}:movienight-backend-staging-${{ steps.version.outputs.version }}
          ${{ steps.account-id.outputs.account_id }}.dkr.ecr.${{ env.AWS_REGION }}.amazonaws.com/${{ env.ECR_REPOSITORY }}:movienight-backend-staging-latest
        cache-from: type=gha
        cache-to: type=gha,mode=max

    - name: Build and push frontend image
      uses: docker/build-push-action@v5
      with:
        context: ./MovieNight/frontend
        push: true
        tags: |
          ${{ steps.account-id.outputs.account_id }}.dkr.ecr.${{ env.AWS_REGION }}.amazonaws.com/${{ env.ECR_REPOSITORY }}:movienight-frontend-staging-${{ steps.version.outputs.version }}
          ${{ steps.account-id.outputs.account_id }}.dkr.ecr.${{ env.AWS_REGION }}.amazonaws.com/${{ env.ECR_REPOSITORY }}:movienight-frontend-staging-latest
        cache-from: type=gha
        cache-to: type=gha,mode=max

    - name: Install Helm
      uses: azure/setup-helm@v3
      with:
        version: 'latest'

    - name: Deployment Decision
      id: deploy-decision
      run: |
        echo "🎯 Version ${{ steps.version.outputs.version }} built and pushed to ECR"
        echo ""
        
        # Check if manual workflow dispatch
        if [ "${{ github.event_name }}" == "workflow_dispatch" ]; then
          DEPLOY_ACTION="${{ github.event.inputs.deployment_action }}"
        else
          # Auto-deploy on push to staging
          DEPLOY_ACTION="deploy"
        fi
        
        echo "action=${DEPLOY_ACTION}" >> $GITHUB_OUTPUT
        
        if [ "$DEPLOY_ACTION" == "build-only" ]; then
          echo "⏹️  Deployment skipped - build only"
          echo "📦 Images are available in ECR:"
          echo "   - movienight-backend-staging-${{ steps.version.outputs.version }}"
          echo "   - movienight-frontend-staging-${{ steps.version.outputs.version }}"
        else
          echo "✅ Proceeding with deployment to staging"
        fi

    - name: Update kubeconfig
      if: steps.deploy-decision.outputs.action == 'deploy'
      run: |
        aws eks update-kubeconfig --name ${{ env.EKS_CLUSTER_NAME }} --region ${{ env.AWS_REGION }}

    - name: Deploy with Helm
      if: steps.deploy-decision.outputs.action == 'deploy'
      run: |
        helm upgrade --install movienight ./MovieNight/helm/movienight \
          --namespace movienight-staging \
          --create-namespace \
          --values ./MovieNight/helm/movienight/values-staging.yaml \
          --set backend.image.tag=movienight-backend-staging-${{ steps.version.outputs.version }} \
          --set frontend.image.tag=movienight-frontend-staging-${{ steps.version.outputs.version }} \
          --set secrets.tmdbApiKey=${{ secrets.TMDB_API_KEY }} \
          --wait \
          --timeout 10m \
          --debug

    - name: Get deployment status
      run: |
        echo "=== Helm Release ==="
        helm list -n movienight-staging
        echo ""
        echo "=== Pods ==="
        kubectl get pods -n movienight-staging
        echo ""
        echo "=== Services ==="
        kubectl get svc -n movienight-staging
        echo ""
        echo "=== Ingress ==="
        kubectl get ingress -n movienight-staging

    - name: Get ALB URL
      run: |
        ALB_URL=$(kubectl get ingress movienight-ingress -n movienight-staging -o jsonpath='{.status.loadBalancer.ingress[0].hostname}' 2>/dev/null || echo "pending")
        echo "🍿 MovieNight Staging deployed!"
        echo "🔗 Access URL: http://${ALB_URL}"
        echo "📊 Release: $(helm list -n movienight-staging -o json | jq -r '.[0].chart')"
        echo "🏷️  Version: ${{ steps.version.outputs.version }}"
