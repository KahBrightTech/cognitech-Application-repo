import { useState } from "react";
import { HeartPulse, LockKeyhole, Mail, Stethoscope, UserRound } from "lucide-react";
export default function LoginScreen({onLogin}){
 const [mode,setMode]=useState("signin"); const [name,setName]=useState(""); const [email,setEmail]=useState(""); const [password,setPassword]=useState(""); const [notice,setNotice]=useState("");
 const submit=e=>{e.preventDefault(); if(!email||!password||(mode==="signup"&&!name)){setNotice("Please complete every field.");return;} localStorage.setItem("lda-demo-user",JSON.stringify({name:name||email.split("@")[0],email})); onLogin({name:name||email.split("@")[0],email});};
 return <main className="auth-shell"><section className="auth-scene"><div className="auth-character"><img src="/assets/characters/dr-mia.svg" alt="Dr. Mia"/><div className="speech-bubble auth-bubble">Welcome! A parent or guardian can sign in here.</div></div><form className="auth-card" onSubmit={submit}><div className="auth-logo"><span><HeartPulse/></span><div><strong>Little Doctor</strong><small>ACADEMY</small></div></div><h1>{mode==="signin"?"Welcome back!":"Create your account"}</h1><p>{mode==="signin"?"Sign in to continue your learning adventure.":"Start your child’s learning journey."}</p>{mode==="signup"&&<label>Your name<div className="input-wrap"><UserRound/><input value={name} onChange={e=>setName(e.target.value)} placeholder="Enter your name"/></div></label>}<label>Email<div className="input-wrap"><Mail/><input type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="parent@example.com"/></div></label><label>Password<div className="input-wrap"><LockKeyhole/><input type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder={mode==="signin"?"Enter your password":"Create a password"}/></div></label>{notice&&<div className="form-notice">{notice}</div>}<button className="primary auth-submit" type="submit"><Stethoscope size={20}/>{mode==="signin"?"Sign in":"Create account"}</button><button type="button" className="text-button auth-switch" onClick={()=>{setMode(mode==="signin"?"signup":"signin");setNotice("");}}>{mode==="signin"?"New here? Create an account":"Already have an account? Sign in"}</button><button type="button" className="forgot-link" onClick={()=>setNotice("Password reset demo: Cognito will email the parent a verification code.")}>Forgot password?</button><small className="demo-note">Demo UI is ready to connect to Amazon Cognito. No password is stored by this demo.</small></form></section></main>
}