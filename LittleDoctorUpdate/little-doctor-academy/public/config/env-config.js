// Local-dev stub. In Docker/Kubernetes this file is regenerated at
// container startup (see docker/50-generate-env-config.sh) from real env
// vars/secrets, overwriting whatever was in the built image at this path.
// For `npm run dev` / `npm run build` outside a container, src/auth/cognito.js
// falls back to import.meta.env.VITE_* (see .env.example) when this is empty.
window.__ENV__ = {};
